/**
 * UpdateMarketDisplayNameDTO
 *
 * Represents the input required to update the user-friendly display name
 * of a market.
 */
export interface UpdateMarketDisplayNameDTO {
  displayName: string;
}